$(document).ready(function () {
    console.log("we made it here in pokemonlist js");
    function capitalizeFirstLetter(string) {
        return string.charAt(0).toUpperCase() + string.slice(1);
    }

    function getPokemonList() {
        console.log("we made it here in pokemonlist js");
    }
    getPokemonList();
})